<form method="post" action="{{ path('pieces_necessaire_delete', {'id': pieces_necessaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pieces_necessaire.id) }}">
    <button class="btn">Delete</button>
</form>

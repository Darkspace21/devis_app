{% extends 'base.html.twig' %}

{% block title %}Garage{% endblock %}

{% block body %}
    <h1>Garage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garage.id }}</td>
            </tr>
            <tr>
                <th>Nom_garage</th>
                <td>{{ garage.nomGarage }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ garage.emplacement }}</td>
            </tr>
            <tr>
                <th>RDV</th>
                <td>{{ garage.RDV ? garage.RDV|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('garage_index') }}">back to list</a>

    <a href="{{ path('garage_edit', {'id': garage.id}) }}">edit</a>

    {{ include('garage/_delete_form.html.twig') }}
{% endblock %}

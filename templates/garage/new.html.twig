{% extends 'base.html.twig' %}

{% block title %}New Garage{% endblock %}

{% block body %}
{{ parent() }}
    <h1>Ajouter un nouveau garage</h1>

    {{ include('garage/_form.html.twig') }}
    {# {{ include('taux_horaire/_form.html.twig') }} #}

    <a href="{{ path('garage_index') }}">back to list</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>Votre compte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>********</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ user.adresse }}</td>
            </tr>
            <tr>
                <th>Gamme_produit</th>
                <td>{{ user.gammeProduit }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-primary">Editer</a>
    <button type="button" class="btn btn-success">Créer un devis <a href=""></a></button>
    <a href="{{ path('vehicule_new') }}"class="btn btn-primary">ajouter une voiture</a>

{% endblock %}

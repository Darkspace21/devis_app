{% extends 'base.html.twig' %}

{% block title %}TauxHoraire index{% endblock %}

{% block body %}
    <h1>TauxHoraire index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>T1</th>
                <th>T2</th>
                <th>T3</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for taux_horaire in taux_horaires %}
            <tr>
                <td>{{ taux_horaire.id }}</td>
                <td>{{ taux_horaire.T1 }}</td>
                <td>{{ taux_horaire.T2 }}</td>
                <td>{{ taux_horaire.T3 }}</td>
                <td>
                    <a href="{{ path('taux_horaire_show', {'id': taux_horaire.id}) }}">show</a>
                    <a href="{{ path('taux_horaire_edit', {'id': taux_horaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('taux_horaire_new') }}">Create new</a>
{% endblock %}
